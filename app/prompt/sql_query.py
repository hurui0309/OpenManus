"""SQL查询相关提示词模块。

本模块定义了用于SQL查询、生成和审查的系统提示词和用户提示词模板。
"""

# SQL查询系统提示词
SQL_QUERY_SYSTEM_PROMPT = """你是一个专业的SQL专家和数据库顾问，擅长SQL语句的分析、优化和问题诊断。

你的主要职责包括：
1. SQL语句审查和优化建议
2. 基于SQL语句生成测试数据
3. 分析SQL执行计划和性能
4. 识别潜在的安全风险和语法错误
5. 提供最佳实践建议

你需要支持多种数据库类型：MySQL、Hive、PostgreSQL、Oracle等。

请根据用户的具体需求，提供准确、专业、易于理解的建议和解决方案。
"""

# SQL查询用户提示词模板
SQL_QUERY_USER_PROMPT = """请分析以下SQL语句：

数据源类型：{datasource_type}
数据源名称：{datasource_name}
任务类型：{task_type}

SQL语句：
```sql
{sql_content}
```

请根据任务类型执行相应的操作：

如果是SQL审查（review）：
1. 检查SQL语法是否正确
2. 分析SQL执行效率和性能
3. 识别潜在的安全风险
4. 提供优化建议
5. 检查是否符合最佳实践

如果是数据生成（data_generation）：
1. 分析SQL语句涉及的表结构
2. 理解查询逻辑和数据关系
3. 生成符合业务逻辑的测试数据
4. 确保数据的一致性和完整性
5. 提供数据插入语句

请提供详细的分析结果和具体的建议。
"""

# SQL审查专用提示词
SQL_REVIEW_PROMPT = """作为SQL审查专家，请对以下SQL语句进行全面审查：

数据源：{datasource_name} ({datasource_type})

SQL语句：
```sql
{sql_content}
```

请从以下方面进行审查：

## 1. 语法检查
- SQL语法是否正确
- 是否符合目标数据库的语法规范
- 关键字使用是否得当

## 2. 性能分析
- 是否存在性能瓶颈
- 索引使用情况
- JOIN操作是否合理
- WHERE条件是否有效

## 3. 安全审查
- 是否存在SQL注入风险
- 权限控制是否合理
- 敏感数据处理是否得当

## 4. 最佳实践
- 代码风格是否规范
- 命名是否合理
- 是否遵循数据库设计原则

## 5. 优化建议
- 具体的优化方案
- 替代实现方法
- 性能提升预期

请提供详细的审查报告和具体的改进建议。
"""

# 数据生成专用提示词
DATA_GENERATION_PROMPT = """作为数据生成专家，请基于以下SQL语句生成相应的测试数据：

数据源：{datasource_name} ({datasource_type})

SQL语句：
```sql
{sql_content}
```

请执行以下任务：

## 1. 表结构分析
- 识别SQL中涉及的所有表
- 分析表之间的关系
- 理解字段类型和约束

## 2. 数据逻辑分析
- 理解SQL的业务逻辑
- 分析数据之间的关联关系
- 识别关键业务规则

## 3. 测试数据生成
- 生成符合业务逻辑的测试数据
- 确保数据的一致性和完整性
- 覆盖各种测试场景（正常、边界、异常）

## 4. 插入语句生成
- 为每个相关表生成INSERT语句
- 考虑外键约束和数据依赖
- 支持批量插入优化

## 5. 数据验证
- 提供数据验证SQL
- 确保生成的数据满足预期
- 提供数据清理脚本

请生成完整的测试数据方案，包括INSERT语句和验证脚本。

特别注意：
- 如果是Hive表，请检查是否为分区表，并生成相应的分区插入语句
- 确保生成的数据在目标数据源中可以正常执行
- 提供多样化的测试数据以覆盖不同的测试场景
"""

# 分区表处理提示词
PARTITION_TABLE_PROMPT = """对于Hive分区表的特殊处理：

当检测到SQL涉及Hive分区表时，请执行以下步骤：

## 1. 分区检测
- 检查表是否为分区表
- 识别分区字段和分区类型
- 分析分区策略

## 2. 分区数据生成
- 为不同分区生成相应的数据
- 确保分区字段值的合理性
- 考虑分区剪枝优化

## 3. 插入语句适配
- 使用 INSERT INTO TABLE ... PARTITION(...) 语法
- 正确指定分区值
- 支持动态分区插入

示例格式：
```sql
-- 静态分区插入
INSERT INTO TABLE table_name PARTITION(ds='20240101')
VALUES (...);

-- 动态分区插入
SET hive.exec.dynamic.partition=true;
SET hive.exec.dynamic.partition.mode=nonstrict;
INSERT INTO TABLE table_name PARTITION(ds)
SELECT ..., '20240101' as ds FROM source_table;
```

请确保生成的语句符合Hive分区表的语法要求。
"""
